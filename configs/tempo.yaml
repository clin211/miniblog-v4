server:
  http_listen_address: 0.0.0.0:3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4319

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo
